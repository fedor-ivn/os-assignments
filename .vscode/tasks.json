{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run single file C program",
            "type": "shell",
            "command": "gcc -Wall -pthread ${file} -o /tmp/main && /tmp/main",
            "problemMatcher": []
        }
    ]
}
